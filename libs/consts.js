export default {
  whiteOrigins: [
    'http://localhost:9000',
    'http://localhost:3000',
    // 以下两个是在 LeanCloud 中配置的 host，xxx 替换为自己的域名
    'http://stg-[appName].leanapp.cn',
    'http://[appName].leanapp.cn'
  ]
}